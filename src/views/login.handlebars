<head>
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap"
    rel="stylesheet"
  />
  <style>
    body { font-family: 'Poppins', sans-serif; background-color: #f4f7f6; }
    .form-group label { font-weight: 600; color: #333; } .form-control {
    border-radius: 10px; padding: 12px; font-size: 16px; border: 2px solid #ddd;
    background-color: #ffffff; } .form-control:focus { border-color: #888;
    box-shadow: 0 0 5px rgba(0, 0, 0, 0.1); } .input-group-text {
    background-color: #f2f2f2; color: #333; border: none; border-radius: 10px 0
    0 10px; } .btn-success { background-color: #007bff; border: none; color:
    white; padding: 12px; font-size: 18px; border-radius: 8px; transition:
    background-color 0.3s ease; } .btn-success:hover { background-color:
    #0056b3; } h1 { background-color: #007bff; padding: 20px; text-align:
    center; color: white; width: 100%; border-radius: 10px 10px 0 0; font-size:
    24px; } .form-container { width: 380px; background-color: #ffffff; padding:
    30px; border-radius: 10px; box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.1); }
    .form-container .m-2 { margin: 10px 0; } .forgot-password { text-align:
    center; font-size: 14px; color: #007bff; cursor: pointer; transition: color
    0.3s ease; } .forgot-password:hover { color: #0056b3; } .text-center {
    text-align: center; }
  </style>
</head>

<main class="d-flex flex-column align-items-center w-100 flex-grow-1">
  <h1>LOGIN FORM</h1>
  <div class="form-container">
    <form
      class="m-3 d-flex flex-column justify-content-evenly flex-wrap align-items-center"
    >
      <div class="form-group w-100">
        <label for="email">Email</label>
        <div class="input-group">
          <span class="input-group-text"><i
              class="bi bi-envelope-fill"
            ></i></span>
          <input
            class="form-control"
            type="email"
            name="email"
            id="email"
            placeholder="Enter your email"
          />
        </div>
      </div>
      <div class="form-group w-100">
        <label for="password">Password</label>
        <div class="input-group">
          <span class="input-group-text"><i class="bi bi-lock-fill"></i></span>
          <input
            class="form-control"
            type="password"
            name="password"
            id="password"
            placeholder="Enter your password"
          />
        </div>
      </div>
      <input
        class="btn btn-success w-100 mt-3"
        type="button"
        id="login"
        value="LOGIN!"
      />
      <div class="text-center mt-3">
        <span class="forgot-password">Forgot your password?</span>
      </div>
    </form>
  </div>
</main>

<script
  src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"
></script>
<script src="/scripts/login.js"></script>