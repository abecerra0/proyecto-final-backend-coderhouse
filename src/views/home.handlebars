<h1> Productos </h1>

<div class="contenedorCards">
  {{#each productos}}
    <div>
      <p> {{this.title}} </p>
      <p> {{this.description}} </p>
      <p> {{this.price}} </p>
      <p> {{this.stock}} </p>
    </div>
  {{/each}}
</div>