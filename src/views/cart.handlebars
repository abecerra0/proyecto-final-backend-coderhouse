<h1> Productos en carrito </h1>

<div class="contenedorCards">
  {{#each productos}}
    <div>
      <p> {{product.title}} </p>
      <p> {{product.description}} </p>
      <p> {{product.price}} </p>
      <p> {{product.stock}} </p>
    </div>
  {{/each}}
</div>